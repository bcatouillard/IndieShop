{% extends "base.html.twig" %}
{% block title %} {{ parent() }} Portabilité {% endblock %}
{% block style %} <link href="css/form.css" rel="stylesheet"> {% endblock %}
{% block contenu %} 
{% if form.valide is defined %} 
    {% if form.valide == true %}
       <h1 class="h3 mb-3 font-weight-normal">{{ form.message }}</h1>
    {% else %}  
       <h1 class="alert alert-danger" role="alert">{{ form.message }}</h1> 
    {% endif %}         
  {% endif %}  
    
    
    <h1 align="center"> Gestion des Portabilités</h1>
    <div class="panel panel-default">
        <div class="table-responsive">
            <form  class="form" action="index.php?page=portable" method="post" enctype="application/x-www-form-urlencoded">
            <table  class="table table-hover table-striped">
                <tr><th>Type</th><th></th><th></th><th></th></tr>
                {% for p in liste %}
                <tr>
                    <td>{{p.type}}</td>
                    <td><div class="btn-group btn-group-lg"><a href="index.php?page=portabilite-modif&id={{p.id}}"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span></a></div></td>
                    <td><div class="btn-group btn-group-lg"><a href="index.php?page=portabilite"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></a></div></td>
                    <td><input type="checkbox" name="cocher[]" id="cocher[]"value="{{p.id}}"/></td>
                </tr> 
                {% endfor %}
            </table>
                <button class="btn btn-lg btn-primary btn-block" type="submit" name="btSupprimer">Supprimer</button>
                {% set nb = form.nbpages %}
                <nav aria-label="Page navigation">
                    <ul class="pagination">
                        <li>
                            <a href="index.php?page=portabilite&nopage={{ 0 }}" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            </a>
                        </li>
                        {% for i in 1..nb %}
                            <li><a href="index.php?page=portabilite&nopage={{ i-1 }}">{{ i }}</a></li>
                        {% endfor %} 
                        <li>
                            <a href="index.php?page=portabilite&nopage={{ nb-1 }}" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                            </a>
                        </li>
                    </ul>
                </nav>
        </div>
    </div>
            </form>
            
                
                                <hr>
                            
   <form  class="form" action="index.php?page=portabilite" method="post" enctype="application/x-www-form-urlencoded">
       <h1 align="center">Ajouter une Portabilité:</h1>
        <label for="type" class="sr-only">Type:</label>
        <input type="textfield" id="type" name="type" class="form-control" placeholder="Type:" required autofocus>
       <button class="btn btn-lg btn-primary btn-block" type="submit" name="btAjouter">Ajouter</button>
   </form>       
    
    
    
{% endblock %}